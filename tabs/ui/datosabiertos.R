datosabiertos <- tabPanel(title = "Datos abiertos", 
                  value = "datosabiertos",
                  br(),
                  HTML("<h1><center>Datos Abiertos <b></b> COVID-19 CUSCO</center></h1>"),
                  column(width = 12,
                         br(), br(),
                         wellPanel(
                           HTML("Aqui puede descargar la informacion estadistica utilizada para los graficos (Próximamente).</h4>")
                         ),
                         br())
)